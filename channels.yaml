# Channels processed by `feed-filter --config channels.yaml`, run on a
# schedule by .github/workflows/update-feeds.yml, which commits the
# regenerated docs/*.xml files back to this (public) repo.
#
# `output` paths are relative to the repo root. Point Feedly (or any other
# reader) at the raw file URL:
#   https://raw.githubusercontent.com/AnotherGuitar/feed-filter/main/<output>
channels:
  - name: thegrayzone
    url: "https://www.youtube.com/@thegrayzone7996/videos"
    min_minutes: 5
    output: docs/thegrayzone.xml
    self_url: "https://raw.githubusercontent.com/AnotherGuitar/feed-filter/main/docs/thegrayzone.xml"
